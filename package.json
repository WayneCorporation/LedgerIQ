{
  "name": "ledgeriq",
  "version": "3.0.0",
  "private": true,
  "description": "A secure invoicing and cash-flow workspace for small businesses.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "restore-backup": "node scripts/restore-backup.js",
    "migrate-postgres": "node scripts/migrate-sqlite-to-postgres.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
